server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mobility_userdb
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always

